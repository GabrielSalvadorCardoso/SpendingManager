<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

        <EditText
            android:id="@+id/edit_text_spent_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="dd/mm/aaaa"/>

        <EditText
            android:id="@+id/edit_text_spent_location"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Endereço"
            android:layout_below="@+id/edit_text_spent_date"/>

        <ListView
            android:id="@+id/list_view_items"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="16dp"
            android:layout_below="@+id/edit_text_spent_location">
        </ListView>

        <Button
            android:id="@+id/button_new_item"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Add Item"
            android:layout_alignParentRight="true"
            android:layout_alignParentBottom="true"
            android:layout_marginBottom="16dp"
            android:layout_marginRight="16dp"
            android:elevation="6dp"
            android:textColor="#ffffff"
            android:textSize="30sp"
            android:background="@drawable/add_item_background">
        </Button>

    <!--</LinearLayout>-->

</RelativeLayout>